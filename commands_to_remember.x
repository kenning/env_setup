## iterm2 
  * has history built in: cmd shift h
## vim
  * q/ q? q: will show you the last few commands you've done of that type
  * zt, zb, z. will move the screen so the cursor is at that point in the screen 
  * in visual mode, :w !pbcopy will copy whatever's selected to your clipboard (on mac)
